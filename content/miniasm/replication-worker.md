## replication-worker

Consumes topology change events and replicates to a secondary store.
Where you learn that "exactly-once" is mostly a marketing term, and what
"at-least-once + idempotent consumer" actually looks like in code.
